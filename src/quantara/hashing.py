"""Hash contract v1 (hash_contract_v1).

SHA-256 identities over raw bytes, checksum artifacts, ZIP members, JCS-
canonicalized descriptor semantics, schema fingerprints, quality identity, and
the row-framed canonical-content hash. Binary floats never enter any payload.
"""
